using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartMenu : MonoBehaviour
{
    void Awake()
    {
        // todo: check to show "New Game" or "Continue". If a level is completed, it can display "Continue". Otherwise, "New Game"
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
